/*--------------------------------------*
  src/components/list/ListView.types.ts
*---------------------------------------*/
import type { RecordItem } from '../../types';

export interface ListViewProps {
  items: RecordItem[];
}